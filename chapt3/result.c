#include <stdio.h>

/**
 * main - afunction to add two values
 * answer: a variable to be added to another variable
 * result: avariable to be added to
 *
 * Return: 0 for success
 */
int main(void)
{
	int answer, result;

	answer = 100;
	result = answer - 10;
	printf("The result is %#x\n", result + 5);

	return (0);
}
